import React from "react";

export default function Home() {
  function fn(n: number) {
    if (n > 5) {
      return true;
    } else {
      return false;
    }
    return true;
  }

  return <h1>Hello Next!</h1>;
}
