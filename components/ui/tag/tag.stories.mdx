import { Meta } from '@storybook/addon-docs';

<Meta
  title="ui/tag"
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# Tag

Tag for categorizing.

## Notes for developers
Tag is rendered as a `span` element

## Examples

```jsx
<Tag variant="pill" color="default">
  welcome
</Tag>;
```